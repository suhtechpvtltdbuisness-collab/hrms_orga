import React, { useMemo, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import toast from 'react-hot-toast';
import { ArrowLeft, Calendar, CheckCircle2, Plus, Users } from 'lucide-react';
import { demoProjects, demoTasks } from '../../../features/projects/demoData';
import { projectOptions } from '../../../features/projects/projectService';

const names = ['Priya Nair', 'Rohan Verma', 'Meera Joshi', 'Amit Sharma'];
const labels = { TODO: 'To do', IN_PROGRESS: 'In progress', IN_REVIEW: 'In review', COMPLETED: 'Completed', BLOCKED: 'Blocked' };
const tone = (x) => ({ COMPLETED:'bg-emerald-50 text-emerald-700', IN_PROGRESS:'bg-blue-50 text-blue-700', IN_REVIEW:'bg-amber-50 text-amber-700', BLOCKED:'bg-rose-50 text-rose-700' }[x] || 'bg-slate-100 text-slate-600');
const date = (x) => x ? new Date(x).toLocaleDateString('en-IN', { day:'numeric', month:'short', year:'numeric' }) : '—';

export default function ProjectDetails() {
  const { id } = useParams(); const project = demoProjects.find((x) => x.id === id) || demoProjects[0];
  const [tab, setTab] = useState('Tasks'); const [tasks, setTasks] = useState(demoTasks.filter((x) => x.projectId === project.id)); const [showTask, setShowTask] = useState(false); const [form, setForm] = useState({ title:'', description:'', priority:'MEDIUM', dueDate:'' });
  const completed = useMemo(() => tasks.filter((x) => x.status === 'COMPLETED').length, [tasks]);
  const createTask = (e) => { e.preventDefault(); if (!form.title.trim()) return; setTasks((old) => [...old, { ...form, id:`demo-task-${Date.now()}`, projectId:project.id, projectName:project.name, status:'TODO', progress:0 }]); setShowTask(false); setForm({ title:'', description:'', priority:'MEDIUM', dueDate:'' }); toast.success('Task created in demo mode'); };
  const update = (taskId, change) => { setTasks((old) => old.map((x) => x.id === taskId ? { ...x, ...change } : x)); toast.success('Task updated in demo mode'); };
  return <div className="min-h-[calc(100vh-88px)] rounded-2xl bg-[#f7f7fa] p-4 sm:p-6"><Link to="/hrms/projects" className="inline-flex items-center gap-2 text-sm font-semibold text-violet-600"><ArrowLeft size={16}/>Back to projects</Link><div className="mt-4 rounded-2xl border border-slate-100 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-2xl font-bold text-slate-900">{project.name}</h1><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${tone(project.status)}`}>{labels[project.status]}</span></div><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">{project.description}</p></div><button onClick={() => setShowTask(true)} className="flex items-center gap-2 rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-semibold text-white"><Plus size={16}/>Add task</button></div><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Progress</p><p className="mt-1 text-lg font-bold">{project.progress}%</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Priority</p><p className="mt-1 text-lg font-bold">{project.priority}</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Due date</p><p className="mt-1 text-sm font-bold">{date(project.dueDate)}</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Task progress</p><p className="mt-1 text-lg font-bold">{completed}/{tasks.length}</p></div></div></div><div className="mt-5 flex gap-1 border-b border-slate-200">{['Overview','Tasks','Members','Activity'].map((x)=><button key={x} onClick={()=>setTab(x)} className={`border-b-2 px-4 py-3 text-sm font-semibold ${tab===x?'border-violet-600 text-violet-600':'border-transparent text-slate-500'}`}>{x}</button>)}</div><section className="mt-4 rounded-2xl border border-slate-100 bg-white p-5 shadow-sm">{tab==='Overview' && <div className="space-y-5"><div><h2 className="font-bold">Project overview</h2><p className="mt-2 text-sm text-slate-600">Owner: {project.owner?.name || '—'} · Started {date(project.startDate)} · Due {date(project.dueDate)}</p></div><div><div className="mb-2 flex justify-between text-sm"><span>Overall progress</span><strong>{project.progress}%</strong></div><div className="h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-violet-600" style={{width:`${project.progress}%`}}/></div></div></div>}{tab==='Tasks' && <div className="space-y-3">{!tasks.length ? <p className="py-10 text-center text-sm text-slate-500">No tasks yet.</p> : tasks.map((t)=><div key={t.id} className="rounded-xl border border-slate-100 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-semibold">{t.title}</p><p className="mt-1 text-sm text-slate-500">{t.description || 'No description'} · Due {date(t.dueDate)}</p></div><select value={t.status} onChange={e=>update(t.id,{status:e.target.value})} className="rounded-lg border px-2 py-1 text-sm">{projectOptions.statuses.map(x=><option key={x} value={x}>{labels[x]}</option>)}</select></div><div className="mt-3 flex items-center gap-3"><input type="range" min="0" max="100" value={t.progress} onChange={e=>update(t.id,{progress:Number(e.target.value)})} className="w-44 accent-violet-600"/><span className="text-xs font-semibold text-violet-600">{t.progress}%</span><span className={`rounded-full px-2 py-1 text-xs font-semibold ${tone(t.status)}`}>{labels[t.status]}</span></div></div>)}</div>}{tab==='Members' && <div className="grid gap-3 sm:grid-cols-2">{names.slice(0, project.memberCount || 2).map((name,i)=><div key={name} className="flex items-center gap-3 rounded-xl border border-slate-100 p-4"><div className="grid h-9 w-9 place-items-center rounded-full bg-violet-100 text-sm font-bold text-violet-700">{name[0]}</div><div><p className="font-semibold">{name}</p><p className="text-xs text-slate-500">{i===0?'Project manager':'Project member'}</p></div></div>)}</div>}{tab==='Activity' && <div className="space-y-4 text-sm text-slate-600"><p><strong>Priya Nair</strong> created the project <span className="float-right text-xs text-slate-400">31 Aug 2026</span></p><p><strong>Priya Nair</strong> added project members <span className="float-right text-xs text-slate-400">31 Aug 2026</span></p><p><strong>Rohan Verma</strong> updated task progress <span className="float-right text-xs text-slate-400">Today</span></p></div>}</section>{showTask && <div className="fixed inset-0 z-[100] grid place-items-center bg-slate-900/40 p-4"><form onSubmit={createTask} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-xl"><h2 className="text-xl font-bold">Add task</h2><label className="mt-4 block text-sm font-medium">Task title<input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="mt-1 w-full rounded-xl border p-2.5"/></label><label className="mt-3 block text-sm font-medium">Description<textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-1 min-h-20 w-full rounded-xl border p-2.5"/></label><div className="mt-3 grid grid-cols-2 gap-3"><label className="text-sm font-medium">Priority<select value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})} className="mt-1 w-full rounded-xl border p-2.5">{projectOptions.priorities.map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm font-medium">Due date<input type="date" value={form.dueDate} onChange={e=>setForm({...form,dueDate:e.target.value})} className="mt-1 w-full rounded-xl border p-2.5"/></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setShowTask(false)} className="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancel</button><button className="rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-semibold text-white">Create task</button></div></form></div>}</div>;
}
